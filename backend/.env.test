# Test Environment Configuration
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ai_interviewer_test
REDIS_URL=redis://localhost:6379/1
SECRET_KEY=test-secret-key-for-testing-only-do-not-use-in-production

# Email Configuration
EMAIL_PROVIDER=console
EMAIL_FROM_ADDRESS=noreply@aiinterviewer.com
EMAIL_FROM_NAME=AI Interviewer

# SendGrid Configuration (optional for testing)
SENDGRID_API_KEY=SG.test-key-only-for-testing

# AWS Configuration (optional for testing)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key

# App Configuration
APP_NAME=AI Interviewer
APP_VERSION=0.1.0
DEBUG=True

# CORS
CORS_ORIGINS='["http://localhost:3000","http://localhost:8000"]'
